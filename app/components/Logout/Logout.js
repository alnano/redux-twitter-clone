import React from 'react';
import PropTypes from 'prop-types';

const Logout = props => {
  return (
    <div>
      <h1>{"Logout"}</h1>
    </div>
  );
};

// Logout.propTypes = {
  
// };

export default Logout;