import React from 'react';
import PropTypes from 'prop-types';
import { formatTimestapm } from 'helpers/utils'
const Duck = props => {
  console.log(props)
  return (
    <div>
      <h1>DUCK</h1>
    </div>
  );
};

Duck.propTypes = {
  
};

export default Duck;